@model SimReg.Web.ViewModels.SIMREG_REQUESTEDBYViewModel
@using (Ajax.BeginForm("Delete", "RequestBy", null,
            new AjaxOptions
            {
                UpdateTargetId = "updateTargetId",
                InsertionMode = InsertionMode.Replace,
                HttpMethod = "POST",
                OnSuccess = "DeleteRequestBySuccess",
            }, new { @id = "deleteRequestByForm" }))
{  

    <div id="updateTargetId" class="error invisible">
    </div>
    @Html.HiddenFor(model => model.REQUESTEDBYID)
    <div class="display-field">
        Do you want delete this data?
    </div>

}