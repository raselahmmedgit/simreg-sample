@model SimReg.Web.ViewModels.SIMREG_DELIVEREDBYViewModel
@using (Ajax.BeginForm("Delete", "DeliverBy", null,
            new AjaxOptions
            {
                UpdateTargetId = "updateTargetId",
                InsertionMode = InsertionMode.Replace,
                HttpMethod = "POST",
                OnSuccess = "DeleteDeliverBySuccess",
            }, new { @id = "deleteDeliverByForm" }))
{  

    <div id="updateTargetId" class="error invisible">
    </div>
    @Html.HiddenFor(model => model.DELIVEREDBYID)
    <div class="display-field">
        Do you want delete this data?
    </div>

}