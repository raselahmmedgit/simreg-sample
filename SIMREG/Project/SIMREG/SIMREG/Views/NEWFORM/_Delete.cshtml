@model SIMREG.ViewModels.SIMREG_NEWFORMViewModel
@using (Ajax.BeginForm("Delete", "NEWFORM", null,
            new AjaxOptions
            {
                UpdateTargetId = "updateTargetId",
                InsertionMode = InsertionMode.Replace,
                HttpMethod = "POST",
                OnSuccess = "DeleteNEWFORMSuccess",
            }, new { @id = "deleteNEWFORMDailog" }))
{  
    
    <fieldset>
        <legend>Category:</legend>
        <div id="updateTargetId" class="error invisible">
        </div>
        @Html.HiddenFor(model => model.ID)
        <div class="display-field">
            Do you want delete this data?
        </div>
    </fieldset>
}